
#include "stringifier_target.h"

// v8::Persistent<v8::Function> StringifierTarget::sort;

void StringifierTarget::Init() {
  // NanScope();
  // v8::Local<v8::String> sortCode = NanNew("(function(array) {array.sort(); })");
  // v8::Local<v8::Value> sortScript = NanCompileScript(sortCode)->Run();
  // v8::Local<v8::Function> sortFn = v8::Local<v8::Function>::Cast(sortScript);
  // NanAssignPersistent(StringifierTarget::sort, sortFn);
}


